'use client';

/**
 * KPI／異常工時審核骨架（Phase 2 注入點）。
 *
 * 業務意圖：集中展示 `MesAbnormalClaim` 待審隊列、核銷操作與關鍵產能 KPI，
 * 服務管理員與老闆角色的事後審計與決策，而不阻塞主看板交互。
 *
 * @returns 目前為佔位；後續由你提供的 React UI 替換實作。
 */
export default function KpiReview() {
  return null;
}
