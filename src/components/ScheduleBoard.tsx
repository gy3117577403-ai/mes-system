'use client';

/**
 * 排產日曆／產線負載看板骨架（Phase 2 注入點）。
 *
 * 業務意圖：從 `src/app/page.tsx` 遷出「週排日曆、產能占用、DeepSeek 排產結果回寫展示」等邏輯，
 * 與車間執行視圖分離，便於計劃員角色獨立迭代與測試。
 *
 * @returns 目前為佔位；後續由你提供的 React UI 替換實作。
 */
export default function ScheduleBoard() {
  return null;
}
