【为什么浏览器显示“拒绝连接”？】

1. 常见原因：只输入了 127.0.0.1 或 localhost，没有带端口。
   浏览器会访问默认的 80 端口，而本项目的开发服务不在 80 端口，所以会 ERR_CONNECTION_REFUSED。

2. 正确做法：在地址栏完整输入（复制下面整行）：
   http://127.0.0.1:3456

3. 使用前请先在本项目文件夹打开终端，执行：
   npm run dev
   等出现 “Ready” 后再用浏览器打开上面的地址。

4. 也可双击运行：打开本地页面.bat（会自动用默认浏览器打开正确网址）
